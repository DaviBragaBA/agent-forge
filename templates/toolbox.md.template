# 🧰 Toolbox: {{nome_amigavel}}

> **Contrato de autorização** — camada de controlo e segurança.
> O runtime **BLOQUEIA** qualquer ferramenta que não esteja aqui.

---

## ✅ Ferramentas Permitidas

{{#each toolbox}}
- ✅ `{{this}}`
{{/each}}

---

## 📐 Princípio

| Camada | Pergunta |
|--------|----------|
| **Skill** | O agent sabe fazer isto? |
| **Toolbox** | O agent pode fazer isto **agora**? |

> Toda ferramenta em `skills.md` que **não** estiver listada acima será **REJEITADA** com erro `tool_not_allowed`.

---

## 📡 Auditoria

Cada chamada é registada em `trace.json`:

```yaml
timestamp: iso8601
argumentos: { ... }
resultado: { ... }
latencia_ms: number
status: sucesso | falha
```
