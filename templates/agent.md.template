# 📋 Agent: {{nome_amigavel}}

> **Contrato de identidade** — define quem o agent é, o que faz e o que entrega ao utilizador.

---

## 📋 Identidade

| Campo | Valor |
|-------|-------|
| **Nome técnico** | `{{nome}}` |
| **Nome amigável** | {{nome_amigavel}} |
| **Versão** | {{metadata.versao_blueprint}} |
| **Criado em** | {{metadata.criado_em}} |

---

## 🎯 Descrição

{{descricao}}

---

## 🏷️ Tipo

**{{tipo}}** — {{tipo_descricao}}

> O tipo orienta o comportamento social do agent: se pergunta, se decompõe, ou se executa directo.

---

## 🎯 Objetivo

{{objetivo}}

> O objetivo orienta **TODAS** as decisões do agent. Sem objetivo claro, não há agent — há apenas automação.

---

## 📦 Contrato de Saída

| Campo | Valor |
|-------|-------|
| **Formato** | `{{contrato_saida.formato}}` |

**Campos obrigatórios:**

{{#each contrato_saida.campos_obrigatorios}}
- `{{this}}`
{{/each}}

> O runtime só pode `FINALIZAR` quando todos os campos obrigatórios estiverem preenchidos.

---

## 🤖 Modelo

```yaml
provider: {{provider}}    # auto = usa o modelo do Cursor
modelo: {{modelo}}
```

---

## 🏷️ Tags

{{#each metadata.tags}}
- `{{this}}`
{{/each}}

---

## 📎 Metadata

```yaml
criado_por: {{metadata.criado_por}}
versao_blueprint: {{metadata.versao_blueprint}}
```
